# Laravel
/vendor
/node_modules
.env
.env.backup
.env.production
.env.local
.env.*.local
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
_ide_helper.php
Homestead.json
Homestead.yaml

# IDEs
*.sublime-project
*.sublime-workspace
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Docker: .env de la raíz (junto a docker-compose.yml), que ahora es
# también el .env que lee Laravel directamente (ver README, sección
# "Desplegar para uso propio"). Ver .env.example para la plantilla.
.env.docker
docker-compose.override.yml

# OS
*.log
*.tmp

# CSVs de importación con datos reales de la colección (precios, tiendas...)
# preparados en la raíz para subirlos por /games/import: nunca a git.
/import_*.csv

# Claude Code: settings.local.json es local por máquina (permisos, etc.),
# no debe compartirse entre quien clone el repo.
.claude/settings.local.json

# Dumps de pg_dump del servicio "postgres-backup" (ruta por defecto de
# BACKUP_PATH en .env.example): nunca a git.
/backups
