# Backend Laravel
backend/vendor/
backend/.env
backend/.env.local
backend/.env.*.local
backend/storage/
backend/bootstrap/cache/
backend/node_modules/
backend/public/hot
backend/.DS_Store
backend/*.log
backend/Thumbs.db

# Flutter / Dart
mobile/build/
mobile/.dart_tool/
mobile/.flutter-plugins
mobile/.flutter-plugins-dependencies
mobile/.packages
mobile/pubspec.lock (opcional - subir para reproducibilidad)
mobile/.idea/
mobile/.vscode/
mobile/coverage/

# IDEs
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Docker: .env de la raíz (junto a docker-compose.yml) con las credenciales/
# puertos reales de Postgres/Redis para docker compose; equivalente a
# backend/.env, mismo motivo para no subirlo. Ver .env.example.
.env
.env.docker
docker-compose.override.yml

# OS
*.log
*.tmp
.env.production

# Desarrollo local
.env.local
.env.*.local

# Claude Code: settings.local.json es local por máquina (permisos, etc.),
# no debe compartirse entre quien clone el repo.
.claude/settings.local.json