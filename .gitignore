# Laravel
/vendor
/node_modules
.env
.env.backup
.env.production
.env.local
.env.*.local
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
_ide_helper.php
Homestead.json
Homestead.yaml

# IDEs
*.sublime-project
*.sublime-workspace
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Docker: .env de la raíz (junto a docker-compose.yml), que ahora es
# también el .env que lee Laravel directamente (ver README, sección
# "Desplegar para uso propio"). Ver .env.example para la plantilla.
.env.docker
docker-compose.override.yml

# Certificados TLS propios para el perfil "prod" de nginx (ver
# docker-compose.yml y docker/nginx.prod.conf): nunca se commitea una
# clave privada.
/certs

# OS
*.log
*.tmp

# Claude Code: settings.local.json es local por máquina (permisos, etc.),
# no debe compartirse entre quien clone el repo.
.claude/settings.local.json
